apply plugin: 'com.android.library'
apply from: "$rootDir$Android"

android library()

dependencies {
    api project(path: ':XMLibs:Center')
    implementation "com.android.support:support-annotations:${SUPPORT_LIBRARY_VERSION}"
}
