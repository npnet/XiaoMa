<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaoma.facerecognize">

    <application
        android:name=".FaceRecognizeApp"
        android:icon="@drawable/icon_face"
        android:label="@string/app_name"
        android:theme="@style/XMPopWindow">

        <meta-data
            android:name="CarType"
            android:value="XMLX" />

        <meta-data
            android:name="CarChannelId"
            android:value="${CAR_CHANNEL_ID}" />

        <meta-data
            android:name="XmGitRev"
            android:value="${XM_GIT_REV}"/>

        <activity android:name=".ui.TipsDialogActivity" />
        <activity android:name=".ui.NavDialogActivity" />
        <activity android:name=".ui.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
