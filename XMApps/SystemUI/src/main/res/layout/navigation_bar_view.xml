<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="ContentDescription">

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/navigation_bar_bg" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingTop="@dimen/navigation_bar_ver_padding"
        android:paddingBottom="@dimen/navigation_bar_ver_padding">

        <ImageButton
            android:id="@+id/btn_back"
            style="@style/navigation_bar_btn"
            android:src="@drawable/navi_back" />

        <ImageButton
            android:id="@+id/btn_home"
            style="@style/navigation_bar_btn"
            android:src="@drawable/navi_home" />
    </LinearLayout>
</merge>